#葫芦娃2D版
观摩了曹老师的示例代码，我在老师代码的基础上完成了第二次作业。
##使用的上节课老师讲的面向对象的设计原则



*里氏替换原则（LSP）

在涉及到类的继承时，考虑了替换原则：子类型（subtype）必须能够替换掉它们的基类型（basetype）。


*依赖倒置原则（DIP）

高层模块不应该依赖底层模块，两者都应该依赖其抽象；
抽象不应该依赖细节；
细节应该依赖抽象。


*单一职责原则（SRP）

应该有且仅有一个原因引起类的变更。
